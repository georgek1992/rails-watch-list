<div class="navbar">
  <h1><strong>WATCH LIST</strong></h1>
    <div>
      <%= link_to "View lists", lists_path, class: "btn btn-primary" %>
      <%= link_to "Create new", new_list_path, class: "btn btn-primary" %>
    </div>
</div>

<div class="banner" >
<h1>Film Categories</h1>
</div>


<div class="container">
  <% @lists.each do |list| %>
    <div class="card mt-2">
      <%= link_to list_path(list) do %>
      <h2 class="card-title text-center"><%= list.name %></h2>
    </div>
    <% end %>
  <% end %>
  <%= link_to "Create new", new_list_path, class: "btn btn-primary mt-2 text-center" %>
</div>

